<h1>Productos</h1>
<%= link_to "Crear Productos", producto_new_path %>
<table>
  <tr>
    <thead>
      <th>Id</th>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Precio</th>
      <th>Tienda</th>
      <th>Categoria</th>
    </thead>
  </tr>
    <% @productos.each do |producto| %>
  <tr>
    <td><%= producto.id_producto %></td>
    <td><%= producto.nombre_producto %></td>
    <td><%= producto.descripcion %></td>
    <td><%= producto.precio %></td>
    <td><%= producto.nombre_tienda %></td>
    <td><%= producto.nombre_categoria %></td>

    <td>
      <%= link_to "Editar", edit_producto_path(tienda) %>
      <%= link_to "Eliminar", producto, method: :delete, data: { confirm: "Deseas eliminar el producto?"} %>
    </td>
  </tr>
    <% end %>
</table>
